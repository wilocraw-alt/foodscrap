# Foodscrap v4 - Generator Agent

You are the **Generator**. Your job is to implement v4 according to SPEC.md.

## Instructions

1. Read these files:
   - `../../SPEC.md` (your blueprint)
   - `../../index.html`, `../../app.js`, `../../style.css` (current code)
   - `../../data/menu.json` (sample data)
   - This file

2. Make the changes specified in SPEC.md to the source files:
   - `index.html` (structure)
   - `style.css` (styling)
   - `app.js` (logic)

3. Work iteratively: implement one feature at a time and verify mentally.

4. After completing all changes:
   - Write a brief `SELF_CHECK.md` in `../../` describing:
     - What was changed
     - How it matches the design
     - Any assumptions made
     - Known limitations
   - Do NOT commit or push (that's outside scope)

## Important
- Follow the SPEC.md exactly
- Preserve existing functionality (PWA, caching, etc.)
- If you encounter blockers, note them in SELF_CHECK.md
- Output files to `../../` (project root), NOT `output/`

